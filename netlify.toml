# Netlify build config for the ProwasappAI frontend.
# The backend (Express + Baileys + WhatsApp) cannot run on Netlify and must
# be hosted elsewhere (VPS, Render, Fly.io, Railway). Set NEXT_PUBLIC_API_URL
# in Netlify → Site settings → Environment variables to point at it.

[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/.next"

[build.environment]
  NODE_VERSION = "20"
  # Production backend (Hostinger VPS, srv1305401.hstgr.cloud).
  # Override in Netlify → Site settings → Build & deploy → Environment if
  # you change hosts; an env-var of the same name in netlify.toml beats the
  # UI value, so the source of truth lives here.
  NEXT_PUBLIC_API_URL = "https://srv1305401.hstgr.cloud"

[[plugins]]
  package = "@netlify/plugin-nextjs"
